# CITY OS 精华提炼 → 苏州明联销售驾驶舱 落地建议

> 用途:本文件供"明联首期销售"项目的 AI 接手使用。上半部分是从同事的 CITY OS
> 项目提炼出的可复用精华点,下半部分是针对本项目的落地建议。
> 核心结论:**别重建地图,把精力放在"让这 10 栋门店建筑自己会说话"上。**

---

## 第一部分 · CITY OS 项目精华抽取(可复用点)

### 1. 产品思路精华(最该抄的部分)

1. **"世界先行"的产品哲学(最高价值)**
   核心定位:不是给城市做 AI Chatbot,而是先建立一个可理解、可模拟的城市世界,再让 Agent 在这个世界里工作。
   本质是先构建 World Model,再放智能体。几乎可无条件迁移到任何"AI 决策/模拟系统"产品。

2. **"全城低分辨率 + 场景高分辨率"的分层策略**
   不追求全局精细,把算力集中在用户会聚焦的场景上。本质是"资源聚焦"。

3. **语义对象化:3D 从"看"升级为"查"**
   每个 POI 不只是几何体,而是带数据的语义对象(名字 + 类型 + 实时数据)。
   这是"数字孪生"和"纯 3D 炫技"的分水岭。

4. **用"详尽的模拟数据"制造完整感**
   36 栋楼 × 3~14 层 × 8 窗 = 2500+ 窗户、45 家品牌、15 种办公状态。
   数据虽是模拟的,但"规模感"换来"万物皆可查"的可信度。**真实性不足时,用完整度补。**

5. **自然语言驱动场景引擎**
   "广州塔跨年活动提前散场" → 自动解析 → Agent 生成预案。把复杂推演封装成自然语言入口。

### 2. 技术实现精华(每条带适用前提)

| 技巧 | 做法 | 适用前提 |
|------|------|----------|
| 程序化城市生成 | 基于分区参数化生成建筑,零美术资产 | 无美术团队、需动态变化、可接受风格化 |
| Canvas 逐像素纹理 | 每栋楼独立窗户用 Canvas 画;办公内景用 Canvas 2D 程序化绘制 | 内容需动态变化、追求轻量 |
| 定制 Shader | 菲涅尔大气辉光、实时 HSL 颜色循环灯光秀 | 需特定视觉且愿意写 GLSL |
| 光轨车辆 | 用"光轨"代替实体车模型沿曲线行驶 | 夜景/科技感场景 |
| Raycaster 业务定位 | hitpoint → worldToLocal → 楼层(Y) + 窗列(X/Z) 双维度定位 | 任何"点击 3D 对象映射业务数据"场景,通用 |
| 电影级镜头开场 | 太空俯冲 → 白色闪光 → 切入城市 | 展示型 demo;工具型产品长开场碍事 |

### 3. 性能优化的具体取舍(可直接照搬)

- **移除 Reflector(实时镜面反射)→ 金属质感水面**:实时反射需 GPU 双重渲染,是 3D 场景最常见性能杀手。
- **Boot 动画用 setTimeout 代替 requestAnimationFrame**:避免后台标签页 rAF 暂停导致动画卡死。
- **窗户纹理用 NearestFilter**:大量小纹理时的通用采样优化。
- **关键词匹配用字符长度加权**:避免短词误匹配。

### 4. 可直接照搬的"原则 → 做法"浓缩清单

```
1. 先建世界,再放 Agent —— 任何 AI 决策系统,先做可推演的世界状态,再让智能体在其中行动
2. 用程序化生成替代美术资产 —— 没有美术团队也能做复杂 3D,且天然支持动态变化
3. 给几何体挂语义数据 —— 3D 的价值不在"好看",在"每个对象都可被点击、查询、推演"
4. 用"全城低分辨率 + 聚焦高分辨率"分层 —— 把算力投在用户会看的地方
5. 真实性不足时用完整度补 —— 模拟数据只要足够详密,就能制造"万物皆可查"的可信感
6. 用 Canvas 2D 程序化画纹理/内景 —— 轻量、可动态、无需外部资源
7. 用"光轨/辉光/雾效/星空"营造氛围 —— 后处理和视觉语言是品质感主要来源,不是模型精度
8. 电影级开场动画制造第一印象 —— demo 的"高级感"很大程度来自开场 30 秒
9. 实时反射是最贵的效果,优先用假反射替代 —— 见一个杀一个
10. 后台标签页会卡死 rAF 动画,计时器逻辑用 setTimeout 兜底
```

---

## 第二部分 · 针对"苏州明联销售驾驶舱"的落地建议

### 项目现状速览

- 苏州 9 区县立体地图(GeoJSON `ExtrudeGeometry`)+ 10 家门店建筑(裙楼 `0.7×0.24×0.7` + 塔楼 `0.45×1×0.45`)
- 门店整体 `grp.scale = 0.6`,高亮 `0.72`;地面辉光 `#22d3a3`;`zoom = 5.0`(范围 `0.5~6`)
- 已有:门店列表联动、发光轮廓线、emissiveMap、AdditiveBlending、数据流、竞品灰色标记
- 核心维度:销量(高亮 `#5eead4`) / 库存 / 利润 / 客户,销量 max 1600

> 关键判断:本项目已经天然走在 CITY OS 的"世界先行"路线上(苏州地图=世界,门店=对象)。
> 该抄的不是"重建世界",而是"如何让这个世界信息密度更高、更实时、更有质感"。

### 高优先级建议(投入小、见效明显)

**1. 让门店建筑用"指标值"驱动高度/辉光 —— 3D 地图变"一眼看分布"的驾驶舱**

源自 CITY OS 的"语义对象化 + 用完整度补真实度"。

现状:门店只有"普通 scale 0.6 / 高亮 0.72"两种状态,信息量浪费。
建议:塔楼 `scale.y` 或地面辉光强度,直接映射当前维度指标值。销量越高的店塔楼越高、辉光越亮。

- 落地:`mesh.scale.y = 0.5 + 0.7 * (指标值 / max)`,辉光 `material.opacity` 同步映射。
- 收益:3D 从"装饰"变成"数据可视化本体"。
- 风险:低,纯前端计算。

**2. 塔楼窗户用 Canvas 程序化画"亮灯窗户" —— 最该直接抄的一招**

源自 CITY OS 的"Canvas 2D 逐像素程序化纹理"。

现状:塔楼 `0.45×1×0.45` 是纯 Box + emissiveMap。
建议:加一层 Canvas 画的窗户纹理,亮灯窗口数 = 销量占比/繁忙度。

- 落地:每栋楼生成一个 Canvas 纹理,亮灯窗数按当前维度指标占比计算,贴到塔楼侧面;纹理采样用 `NearestFilter`。
- 收益:门店"活"起来,且窗户亮灯本身在传递数据。
- 风险:低。

**3. 加 Bloom 后处理 + 雾效 —— 补齐目前缺的"质感天花板"**

源自 CITY OS 的 `UnrealBloomPass + FogExp2`。

现状:有地面辉光、发光轮廓线、AdditiveBlending,但都是"自身发光",没有"溢光"。
建议:加 `EffectComposer + UnrealBloomPass`,让辉光从"发光色块"变成"光晕";再挂 `FogExp2` 让远处门店淡入。

- 落地:单文件从 Three.js CDN 引 postprocessing 模块,`composer` 加 `UnrealBloomPass`。
- 收益:视觉高级感主要来自后处理,不是模型精度。
- 风险:中。`bloomPass.strength` 别开太高;注意 import map 正确。

**4. 加 0.8~1.2 秒"俯冲聚焦"开场,替代生硬的 zoom 定格**

源自 CITY OS 的"电影级开场",但要克制。

现状:`zoom=5.0` 直接定格。
建议:初始化后相机从较低 zoom(如 2.5,看到苏州全貌)动画到 5.0 聚焦核心 2~3 家店,ease-out 缓动。驾驶舱开场必须短,别照搬 CITY OS 长镜头。

- 落地:`animateCamera(2.5 → 5.0, 900ms, easeOutCubic)`,开场期间别拦截点击。
- 收益:第一印象的"高级感"很大程度来自入场。
- 风险:低。

**5. 让"数据流"从静态坐标变成流动光点**

源自 CITY OS 的"光轨车辆"。

现状:数据流是静态坐标。
建议:改成沿路径流动的光点(门店 ↔ 客户/仓储方向),体现"实时数据流动"。

- 落地:`CatmullRomCurve3` 定义路径,光点沿曲线循环移动。
- 收益:低成本换来"实时感"。
- 风险:中。动画计时逻辑用 `setTimeout` 兜底(后台标签页 rAF 会暂停)。

### 中优先级建议(有则更好)

**6. 区县 extrude 深度差异化 —— 让"重点区"在地图上自己凸出来**

源自"全城低分辨率 + 聚焦高分辨率"。
现状:苏州 9 区县统一 extrude。建议按"门店数量/销量"给不同 extrude 深度,姑苏(宝隆★+德宝车业)、吴中、虎丘更高,无门店区更浅。

**7. tab 切换时 3D 门店高度/辉光/窗户一起切换维度**

深化现有 tab 联动。
现状:切 tab 只更新列表指标值。建议切换"销量/库存/利润/客户"时,门店高度、辉光、窗户亮灯数量同步重映射,四维度在地图上实时重演。

### 不建议照搬的(排除清单)

- **CITY OS 长电影镜头 + 地球开场**:本驾驶舱是工具型产品,用户要快速看数据,长开场是负资产。
- **Reflector 实时镜面反射**:CITY OS 明确记下它是性能杀手并已移除,更不该加。
- **窗户级"2500 对象全量模拟"**:本项目只有 10 家门店,不需那种规模,但"窗户亮灯映射数据"的概念可缩放到 10 栋楼用(见建议 2)。

---

## 一句话接手指令

> 别重建地图,把精力放在"让这 10 栋门店建筑自己会说话"——
> 指标驱动的高度/辉光/窗户亮灯(建议 1、2)、Bloom 后处理质感(建议 3)、
> 短开场 + 流动数据流的实时感(建议 4、5)。

*生成日期:2026-08-25*
